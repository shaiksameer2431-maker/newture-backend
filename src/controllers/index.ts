/**
 * Controllers layer — reserved for extracting request handlers from app.ts.
 * Current handlers remain in src/app.ts to preserve exact runtime behavior.
 * Prefer moving endpoint logic here when splitting further by domain
 * (tickets, rules, students, etc.) without changing API contracts.
 */
export {};
