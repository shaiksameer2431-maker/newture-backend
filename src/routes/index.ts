// Route handlers for the Express API.
// Domain routes are registered in ../app.ts (preserved 1:1 from the original monolith).
// Auth routes live in ./authRoutes.js.

export { default as authRoutes } from './authRoutes.js';
